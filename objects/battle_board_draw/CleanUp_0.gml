if (surface_exists(surface)) surface_free(surface)
if (surface_exists(surface_cover)) surface_free(surface_cover)
if (surface_exists(surface_extra)) surface_free(surface_extra)
if (surface_exists(surface_mask)) surface_free(surface_mask)